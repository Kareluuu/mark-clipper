// 统一的类型定义导出文件
// 为项目中的所有类型提供统一的访问入口

// =============================================================================
// 数据库类型导出 - 数据结构的单一来源
// =============================================================================

export type { Database } from './database';

// =============================================================================
// 业务类型导出 - 基于数据库类型的业务逻辑
// =============================================================================

// 类型导出 - 使用 export type
export type {
  // 前端业务类型
  Clip,
  
  // 数据库类型别名（从 database.ts 派生）
  ClipRow,
  ClipInsert, 
  ClipUpdate,
} from './clips';

// 函数导出 - 使用普通 export
export {
  // 类型转换函数
  transformClipRow,
  transformToInsert,
  transformToUpdate,
  transformClipRows,
  
  // 类型验证函数
  isValidClip,
  isValidClipRow,
  validateClips,
} from './clips';

// =============================================================================
// 默认导出
// =============================================================================

// 默认导出主要业务类型
export type { default as ClipDefault } from './clips';
